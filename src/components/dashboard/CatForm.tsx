'use client'; import { type FormEvent, useState } from 'react'; import { createCat, updateCat } from '../../lib/api/cats'; import type { Cat } from '../../lib/types';
export function CatForm({ cat }: { cat?: Cat }) { const [saving, setSaving] = useState(false); const [done, setDone] = useState(false); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const f = new FormData(e.currentTarget); setSaving(true); try { const data = { name: f.get('name'), age_months: Number(f.get('age')), sex: f.get('sex'), city: f.get('city'), state: f.get('state'), sociability: Number(f.get('sociability')), energy: Number(f.get('energy')), playfulness: Number(f.get('playfulness')), castrated: f.get('castrated') === 'on', vaccinated: f.get('vaccinated') === 'on', dewormed: f.get('dewormed') === 'on' }; if (cat) await updateCat(cat.id, data); else await createCat(data); setDone(true); } finally { setSaving(false); } } return <form className="space-y-4 rounded-3xl bg-white p-6 shadow-sm" onSubmit={submit}><input className="w-full rounded-xl border p-3" defaultValue={cat?.name} name="name" placeholder="Name" required /><div className="grid gap-3 sm:grid-cols-2"><input className="rounded-xl border p-3" defaultValue={cat?.age_months} min="0" name="age" placeholder="Age in months" required type="number" /><select className="rounded-xl border p-3" defaultValue={cat?.sex ?? 'female'} name="sex"><option value="female">Female</option><option value="male">Male</option></select><input className="rounded-xl border p-3" defaultValue={cat?.city} name="city" placeholder="City" required /><input className="rounded-xl border p-3" defaultValue={cat?.state} maxLength={2} name="state" placeholder="State" required /></div><div className="grid gap-3 sm:grid-cols-3">{['sociability','energy','playfulness'].map((field) => <label className="text-sm capitalize" key={field}>{field}<input className="mt-1 w-full" defaultValue={(cat?.[field as keyof Cat] as number | undefined) ?? 3} max="5" min="1" name={field} type="range" /></label>)}</div><div className="flex flex-wrap gap-4 text-sm">{['castrated','vaccinated','dewormed'].map((field) => <label key={field}><input defaultChecked={Boolean(cat?.[field as keyof Cat])} name={field} type="checkbox" /> {field}</label>)}</div>{done && <p className="text-teal-800">Cat saved.</p>}<button className="rounded-xl bg-teal-800 px-4 py-3 font-bold text-white" disabled={saving}>{saving ? 'Saving...' : 'Save cat'}</button></form>; }
