export function DonationMap({ cep }: { cep: string }) { return <div className="mt-5 rounded-2xl border border-dashed border-teal-700 bg-teal-50 p-6"><p className="font-bold text-teal-950">Nearby drop-off points{cep ? ` near ${cep}` : ''}</p><p className="mt-2 text-sm text-teal-900">The map with partner locations will appear here once addresses are registered in your area.</p></div>; }
