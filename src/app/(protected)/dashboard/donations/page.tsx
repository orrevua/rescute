'use client'; import { useEffect, useState } from 'react'; import Link from 'next/link'; import { getDonations } from '../../../../lib/api/donations'; import { ProtectedRoute } from '../../../../lib/auth/guard'; import type { DonationPost } from '../../../../lib/types';
export default function DashboardDonationsPage() { const [posts, setPosts] = useState<DonationPost[]>([]); useEffect(() => { void getDonations().then(setPosts); }, []); return <ProtectedRoute requiredRole="protector"><main className="min-h-screen bg-[#f4f7f4] p-8"><div className="mx-auto max-w-5xl"><div className="flex justify-between"><h1 className="text-3xl font-bold">Campaigns</h1><Link className="rounded-xl bg-teal-800 px-4 py-2 font-bold text-white" href="/dashboard/donations/new">New campaign</Link></div><div className="mt-6 space-y-3">{posts.map((post) => <article className="rounded-2xl bg-white p-5 shadow-sm" key={post.id}><h2 className="font-bold">{post.title}</h2><p className="text-stone-600">{post.description}</p></article>)}</div></div></main></ProtectedRoute>; }
